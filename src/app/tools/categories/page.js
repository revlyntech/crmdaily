import Categories from "@/components/tools/Categories";

export const metadata = {
  title: "CRM Categories",
  description: "Every major CRM sorted by what it's actually built for.",
};

export default function Page() {
  return <Categories />;
}
