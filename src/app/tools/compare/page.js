import Compare from "@/components/tools/Compare";

export const metadata = {
  title: "Compare CRMs",
  description: "Compare up to four CRM platforms side by side on price, fit, and tradeoffs.",
};

export default function Page() {
  return <Compare />;
}
