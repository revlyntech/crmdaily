import CRMMatcher from "@/components/tools/CRMMatcher";

export const metadata = {
  title: "CRM Matcher",
  description: "Filter CRMs by category and budget to build a shortlist that fits your team.",
};

export default function Page() {
  return <CRMMatcher />;
}
