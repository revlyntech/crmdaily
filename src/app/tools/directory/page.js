import Directory from "@/components/tools/Directory";

export const metadata = {
  title: "CRM Directory",
  description: "Every CRM platform we track, in one browsable list.",
};

export default function Page() {
  return <Directory />;
}
